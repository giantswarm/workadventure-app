apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: workadventure-psp
  namespace: workadventure
rules:
  - apiGroups:
      - policy
    resources:
      - podsecuritypolicies
    verbs:
      - use
    resourceNames:
      - privileged
